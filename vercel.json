{
    "version": 2,
    "builds": [
        {
            "src": "./backend/app.py",
            "use": "@vercel/python"
        }
    ],
    "routes": [
        {
            "src": "/backend/(.*)",
            "dest": "/backend/"
        }
    ]
}